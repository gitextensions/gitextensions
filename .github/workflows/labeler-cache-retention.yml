name: "Labeler: Cache Retention"

on:
  schedule:
    - cron: "25 20 * * *" # 20:25 every day (arbitrary time daily)

  workflow_dispatch:

jobs:
  cache-retention:
    # Do not run the workflow on forks outside the 'gitextensions' org
    if: ${{ github.repository_owner == 'gitextensions' }}
    uses: dotnet/issue-labeler/.github/workflows/cache-retention.yml@46125e85e6a568dc712f358c39f35317366f5eed # v2.0.0
