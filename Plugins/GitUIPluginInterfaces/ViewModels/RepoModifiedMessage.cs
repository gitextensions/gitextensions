namespace Plugins.GitUIPluginInterfaces.ViewModels;

/// <summary>
/// Represents a typed message which is sent in order to trigger updates on repo modification.
/// </summary>
public sealed record RepoModifiedMessage();
