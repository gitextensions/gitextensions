---
status: accepted
---

# Duke 版版本派生自官方基线

Duke 版使用 `<官方版本>-duke.<修订号>`，Windows 文件版本使用对应的第四段修订号；首个版本为 `7.2.0-duke.1` / `7.2.0.1`，Git 标签为 `v7.2.0-duke.1`。同一官方基线上的 Duke 发布递增修订号，实际吸收并验证新的官方版本后重置为 `.1`。该规则既保留与官方兼容基线的可见关系，又避免占用或冒充官方补丁版本。本 ADR 只确定版本语义；构建、CI 和安装包元数据的接入另行实施。
