---
status: accepted
---

# 在 Dashboard 集成收藏仓库状态总览

Duke 版将跨仓库状态总览直接集成到 Git Extensions Dashboard，并以收藏仓库作为唯一管理边界：收藏是用户已有的显式选择，避免另建项目注册表或扫描文件系统。总览把本地状态检查与网络 Fetch 分开；前者可频繁、离线执行，后者只在用户手动触发或满足空闲调度时访问每个仓库的全部远端。两类错误分别保留，防止一次成功的本地检查掩盖远端更新失败。此选择复用官方仓库历史、设置和视觉组件，但会使 Dashboard 成为与官方同步时需要重点核验的冲突区域。
