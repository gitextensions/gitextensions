# Git Extensions Duke Edition

本仓库在 Git Extensions 官方上游基础上维护 Duke 版个人定制。这里仅定义讨论、开发和维护魔改时使用的统一语言。

## Language

**官方上游**：
`gitextensions/gitextensions` 维护的原始 Git Extensions 项目。
_避免_：原仓库、官方版代码

**Duke 版**：
基于官方上游并包含个人定制的 Git Extensions 发行线，正式名称为 Git Extensions Duke Edition。
_避免_：私版、随意修改版

**官方基线**：
Duke 版已经吸收、并作为魔改差异起点的一个确定官方标签和提交。
_避免_：最新版、当前官方分支

**魔改**：
Duke 版相对官方基线有意保留的产品行为或架构差异。
_避免_：补丁、临时改动

**收藏仓库**：
用户在 Git Extensions 中标记为收藏、因而纳入跨仓库管理范围的本地 Git 仓库。
_避免_：全部仓库、最近仓库

**仓库状态总览**：
按分组集中展示收藏仓库本地状态及其与上游同步关系的 Duke 版首页视图。
_避免_：项目列表、传统视图

**本地状态检查**：
只读取本地 Git 数据以计算分支、工作区和同步状态的操作，不访问远端。
_避免_：刷新远端、Fetch

**后台 Fetch**：
在满足空闲调度条件时，为收藏仓库的全部远端更新本地远端跟踪引用的操作。
_避免_：本地刷新、自动同步

**同步状态**：
本地分支相对其上游分支的关系，包括已同步、领先、落后、已分叉、未设置上游和分离 HEAD。
_避免_：工作区状态、Fetch 状态
