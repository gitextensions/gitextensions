namespace GitExtensions.Plugins.GitImpact.Properties;

internal static class Resources
{
    // The Windows resource generator materializes this PNG as a platform bitmap.
    // The Avalonia plugin host will restore the embedded PNG directly in its UI layer.
    internal static GitExtensions.Shims.WinForms.Image? IconGitImpact => null;
}
