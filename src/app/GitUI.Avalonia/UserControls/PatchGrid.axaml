<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="GitUI.PatchGrid">
  <Grid x:Name="columnsGrid"
        RowDefinitions="Auto,*">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="45" />
      <ColumnDefinition Width="48" />
      <ColumnDefinition Width="50" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="140" />
      <ColumnDefinition Width="37" />
      <ColumnDefinition Width="85" />
    </Grid.ColumnDefinitions>
    <Border x:Name="Status"
            Padding="6,4"
            BorderBrush="{DynamicResource GitExtensionsSectionBorderBrush}"
            BorderThickness="0,0,1,1">
      <TextBlock Text="Status" FontWeight="SemiBold" />
    </Border>
    <Border x:Name="Action"
            Grid.Column="1"
            Padding="6,4"
            BorderBrush="{DynamicResource GitExtensionsSectionBorderBrush}"
            BorderThickness="0,0,1,1">
      <TextBlock Text="Action" FontWeight="SemiBold" />
    </Border>
    <Border x:Name="FileName"
            Grid.Column="2"
            Padding="6,4"
            BorderBrush="{DynamicResource GitExtensionsSectionBorderBrush}"
            BorderThickness="0,0,1,1">
      <TextBlock Text="Name" FontWeight="SemiBold" />
    </Border>
    <Border x:Name="subjectDataGridViewTextBoxColumn"
            Grid.Column="3"
            Padding="6,4"
            BorderBrush="{DynamicResource GitExtensionsSectionBorderBrush}"
            BorderThickness="0,0,1,1">
      <TextBlock Text="Subject" FontWeight="SemiBold" />
    </Border>
    <Border x:Name="authorDataGridViewTextBoxColumn"
            Grid.Column="4"
            Padding="6,4"
            BorderBrush="{DynamicResource GitExtensionsSectionBorderBrush}"
            BorderThickness="0,0,1,1">
      <TextBlock Text="Author" FontWeight="SemiBold" />
    </Border>
    <Border x:Name="dateDataGridViewTextBoxColumn"
            Grid.Column="5"
            Padding="6,4"
            BorderBrush="{DynamicResource GitExtensionsSectionBorderBrush}"
            BorderThickness="0,0,1,1">
      <TextBlock Text="Date" FontWeight="SemiBold" />
    </Border>
    <Border x:Name="CommitHash"
            Grid.Column="6"
            Padding="6,4"
            BorderBrush="{DynamicResource GitExtensionsSectionBorderBrush}"
            BorderThickness="0,0,0,1">
      <TextBlock Text="Commit hash" FontWeight="SemiBold" />
    </Border>

    <ListBox x:Name="Patches"
             Classes="gitextensions-stretch-items"
             Grid.Row="1"
             Grid.ColumnSpan="7"
             Background="{DynamicResource GitExtensionsKnownColorControlLightBrush}"
             SelectionMode="Single">
      <ListBox.Styles>
        <Style Selector="ListBoxItem">
          <Setter Property="Padding" Value="0" />
          <Setter Property="MinHeight" Value="28" />
        </Style>
        <Style Selector="ListBoxItem:selected /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Background" Value="{DynamicResource GitExtensionsHighlightBackgroundBrush}" />
          <Setter Property="Foreground" Value="{DynamicResource GitExtensionsKnownColorHighlightTextBrush}" />
        </Style>
      </ListBox.Styles>
    </ListBox>
  </Grid>
</UserControl>
